apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: monitoring-reader
  namespace: monitoring
rules:
  - apiGroups: [""]
    resources: 
      - pods
      - services
    verbs:
      - get
      - list
      - watch
